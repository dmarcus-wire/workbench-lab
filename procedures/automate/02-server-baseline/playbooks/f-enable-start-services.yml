---
- name: enable and start services
  hosts: host
  become: true
  gather_facts: false

  tasks:

    - name: enable and start services
      service:
        name: "{{ services }}"
        state: started
        enabled: true
      ignore_errors: true
...